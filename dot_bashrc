
##-----------------------------------------------------
## synth-shell-greeter.sh
if [ -f /home/tiam/.config/synth-shell/synth-shell-greeter.sh ] && [ -n "$( echo $- | grep i )" ]; then
source /home/tiam/.config/synth-shell/synth-shell-greeter.sh
fi

##-----------------------------------------------------
## synth-shell-prompt.sh
if [ -f /home/tiam/.config/synth-shell/synth-shell-prompt.sh ] && [ -n "$( echo $- | grep i )" ]; then
	source /home/tiam/.config/synth-shell/synth-shell-prompt.sh
fi

##-----------------------------------------------------
## better-ls
if [ -f /home/tiam/.config/synth-shell/better-ls.sh ] && [ -n "$( echo $- | grep i )" ]; then
	source /home/tiam/.config/synth-shell/better-ls.sh
fi

##-----------------------------------------------------
## alias
if [ -f /home/tiam/.config/synth-shell/alias.sh ] && [ -n "$( echo $- | grep i )" ]; then
	source /home/tiam/.config/synth-shell/alias.sh
fi

##-----------------------------------------------------
## better-history
if [ -f /home/tiam/.config/synth-shell/better-history.sh ] && [ -n "$( echo $- | grep i )" ]; then
	source /home/tiam/.config/synth-shell/better-history.sh
fi

# custom alias

alias rm='trash -v'
alias dnf="sudo dnf5 "


# idk what that is
. "$HOME/.cargo/env"
